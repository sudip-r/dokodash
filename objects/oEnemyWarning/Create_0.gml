// Compatibility only. Warnings now use warning_x instead of lanes.
lane = 1;
lane_x = [180, 360, 540];
warning_x = x;

warning_timer = 45;
blink_timer = 0;
spawn_y = -100;

target_enemy = oThugChaser;
